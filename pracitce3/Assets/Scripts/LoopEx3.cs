using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LoopEx3 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        for (int i = 10; i <= 50; i += 2)
        {
            print(i);
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
